<nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">MRM</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item ms-auto">
            <a class="nav-link" href="/products">All Products</a>
          </li>
          <% if (isAdmin) { %>
            <li class="nav-item ms-auto">
                <a href="/admin/add" class="nav-link">Add Product</a>
            </li>
            <li class="nav-item ms-auto">
                <a href="/admin/finishedproducts" class="nav-link">Out Of Stock</a>
            </li>
          <% } %>
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">  
            <% if (isAdmin) { %>
                <li class="nav-item ms-auto fw-bold">
                    <a class="nav-link" href="/admin/orders">All Orders</a>
                </li>
            <% } %>
        
        
            <% if (!currentUser && !isAdmin) { %>
                <li class="nav-item ms-auto fw-bold">
                    <a class="nav-link" href="/login">Login</a>
                </li>
                
                <li class="nav-item ms-auto fw-bold">
                    <a class="nav-link" href="/register">Register</a>
                </li>
            <% } else if (!isAdmin && currentUser) { %>
                <li class="nav-item ms-auto fw-bold">
                    <a href="/myorders" class="nav-link">My Orders</a>
                </li>
                <form action="/logout" method="post">
                    <li class="nav-item ms-auto fw-bold">
                        <button class="nav-link btn fw-bold" type="submit">Logout</button>
                    </li>
                </form>
            <% } %> 
            <form class="d-flex" action="/search" method="post">
                <input class="form-control" name="term" id="term" type="search" placeholder="Search Product Name" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </ul>
      </div>
    </div>
  </nav>